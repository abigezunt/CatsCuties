<h2>Our delicious, gluten-free, vegan, dairy-free, kosher-parve, allergen-friendly, dark chocolates</h2>

<% if notice %>
  <p id="notice"><%= notice %></p>
<% end %>

<% cache ['store', Product.latest] do %>
	<div class="row">
	<% @products.in_groups_of(3, false).each do |group|%>
	  <div class="row">
	  	<div class="three columns">
	  		<% group.each do |product| %>
	  		  <% cache ['entry', product] do %>
	  		    <%#= render "products/product" if product %>


						<div class="col-sm-6 col-md-4">
						  <div class="thumbnail">
						    <%= image_tag product.image_url, class: "img-thumbnail" %>
						    <div class="caption">
						      <h3><%= product.name %></h3>
						      <p><%= sanitize(product.description) %></p>
						      <span class="price"><%= number_to_currency(product.unit_price) %></span>

						      <p>
						      <%= link_to "Ingredients", product_path(product), class: "btn btn-primary" %>
						      <%= button_to "Add to Cart", line_items_path(product_id: product), {class: "btn btn-primary"}  %>
						      </p>
						    </div>
						  </div>
						</div>


	  		  <% end %>
	  		<% end %>
	  	</div>
	  </div>
	<% end %>
	</div>
<% end %>