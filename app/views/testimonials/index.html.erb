<div class="container">

<h3>TESTIMONIALS FOR CAT’S CUTIES EXTRAORDINARY CHOCOLATES</h3>


<% @testimonials.in_groups_of(2, false).each do |group| %>
<div class="row">
	<% group.each do |testimonial| %>
	  <div class="col-xs-6 col-md-4">
      <div class="thumbnail">	    
	      <p>
		    <%= testimonial.quote %> - <%= testimonial.name %>
		    <% if testimonial.comment %>
		      <%= testimonial.comment %>
		    <% end %>
		    </p>
	    </div>
	  </div>
	<% end %>
</div>

<% end %>
</div>
